# owdle
guessing game for overwatch characters

## overview
Base web app (vanilla JS + Vite) for a Wordle-like Overwatch guessing game.

## setup
```bash
npm install
npm run dev
```

## data
Hero data lives in `src/data/heroes.json` and drives the game.
